/*

1) Crear las funciones correspondientes,
para que el siguiente código funcione apropiadamente:

imprimirSuma(4, 2);             // Imprime 6 en consola
imprimirResta(4, 2);            // Imprime 2 en consola
imprimirMultiplicacion(4, 2);   // Imprime 8 en consola
imprimirDivision(4, 2);         // Imprime 2 en consola

---------------------------------------------------------
2) Adicionar un control de valor:

imprimirDivision(4, 0);         // Imprime "No se puede dividir entre cero" con formato de error

---------------------------------------------------------

3) Adicionar un control de tipo de dato:

imprimirSuma(100, true);            // Imprime "Ambos parámetros deben ser números" con formato de error
imprimirSuma(100, '40');            // Imprime "Ambos parámetros deben ser números" con formato de error
imprimirSuma('100', 40);            // Imprime "Ambos parámetros deben ser números" con formato de error
imprimirResta('100', 40);           // Imprime "Ambos parámetros deben ser números" con formato de error
imprimirMultiplicacion('100', 40);  // Imprime "Ambos parámetros deben ser números" con formato de error
imprimirDivision('100', 40);        // Imprime "Ambos parámetros deben ser números" con formato de error

// Atención:
imprimirDivision('100', 0);         // Imprime "Ambos parámetros deben ser números" con formato de error
                                    // No llega a verificar si el 2do parámetro es cero o no.

*/
